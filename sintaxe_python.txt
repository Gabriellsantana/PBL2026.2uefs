Documento criado para auxilio com sintaxe em python (forma de escrita)

print("idade: ",end="");

O end define o que o print coloca no final da saída. Por padrão ele coloca uma quebra de linha (\n). Eu usei end="" para não quebrar a linha, deixando o usuário digitar a resposta na mesma linha da mensagem.”

OBS:Como  a Materia MI é integrada com algoritmo 1 , a professora resaltu de fazer  saida e entrada de dados com print e input , porque é forma padrão das maiorias das linguagem , sabendo que ptyhon permitr imprimir um texto com comando entrada de dados "input" e por isso também a declaaçaõ devarivel acima do código que não um necesidade do python

Códiogo: metodos   que foi pesquisados , para resolver problemas epecificos e otimizados:(len e isdigit)  len = comprimento  isdigit retorna true ou false caso uma var seja formada apenas por número ou não 

no len - logicamente poderia valida a quantida de caracterios digitados com for pra percorre logicamente esse string e ver quantos caracterios tem ali matricula = 

solução voltada a logica 
input("Matrícula: ")

contador = 0

for caracteres in matricula:
    contador += 1

print("Quantidade de caracteres:", contador)

ai eu jogaria dentro de outro if para valida de contador == 9;

replace - metetodo usa para tratar virgular nos valores float , antende de linguagem transforma e float o replace caso user tenha digitado " , "  troca para "."
                                   